<Window x:Class="CollegeInfoSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CollegeInfoSystem.ViewModels"
        mc:Ignorable="d"
        Title="Login" Height="250" Width="400" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:" Grid.Row="0"/>
        <TextBox Grid.Row="0" Margin="100,0,0,5" Width="200" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Password:" Grid.Row="1" Margin="0,10,0,0"/>
        <PasswordBox Grid.Row="1" Margin="100,10,0,0" Width="200" PasswordChanged="PasswordBox_PasswordChanged"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Login" Command="{Binding LoginCommand}" Width="100" Margin="10"/>
        </StackPanel>
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>
