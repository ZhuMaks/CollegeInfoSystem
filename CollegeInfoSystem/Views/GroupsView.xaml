<UserControl x:Class="CollegeInfoSystem.Views.GroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <StackPanel>

            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox Width="200" Margin="5"
                         Text="{Binding GroupNameFilter, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Пошук за назвою групи"/>

                <ComboBox Width="200" Margin="5"
                          ItemsSource="{Binding Faculties}"
                          DisplayMemberPath="FacultyName"
                          SelectedItem="{Binding SelectedFaculty}"
                          ToolTip="Виберіть факультет"/>

                <ComboBox Width="200" Margin="5"
                          ItemsSource="{Binding Curators}"
                          DisplayMemberPath="FullName"
                          SelectedItem="{Binding SelectedCurator}"
                          ToolTip="Виберіть куратора"/>

                <Button Content="Очистити фільтр" Command="{Binding ClearFiltersCommand}" Width="150" Margin="5"/>
                <Button Content="Звіт у Excel" Command="{Binding ExportToExcelCommand}" Width="150" Margin="5"/>

                <Button Content="Імпорт з Excel" Command="{Binding ImportFromExcelCommand}" Width="150" Margin="5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentUserRole}" Value="admin">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <ListView ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding GroupID}" Width="50"/>
                        <GridViewColumn Header="Назва" DisplayMemberBinding="{Binding GroupName}" Width="150"/>
                        <GridViewColumn Header="Факультет" DisplayMemberBinding="{Binding Faculty.FacultyName}" Width="150"/>
                        <GridViewColumn Header="Куратор" DisplayMemberBinding="{Binding Curator.FullName}" Width="150"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">

                <Button Content="Додати" Command="{Binding AddGroupCommand}" Width="100" Margin="5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentUserRole}" Value="admin">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CurrentUserRole}" Value="teacher">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Оновити" Command="{Binding UpdateGroupCommand}" Width="100" Margin="5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentUserRole}" Value="admin">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CurrentUserRole}" Value="teacher">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Видалити" Command="{Binding DeleteGroupCommand}" Width="100" Margin="5">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentUserRole}" Value="admin">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding CurrentUserRole}" Value="teacher">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>

            <TextBlock Text="Студенти в групі" FontWeight="Bold" Margin="10"/>
            <ListView ItemsSource="{Binding StudentsInGroup}" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding StudentID}" Width="50"/>
                        <GridViewColumn Header="Ім'я" DisplayMemberBinding="{Binding FirstName}" Width="100"/>
                        <GridViewColumn Header="Прізвище" DisplayMemberBinding="{Binding LastName}" Width="100"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </StackPanel>
    </Grid>
</UserControl>
